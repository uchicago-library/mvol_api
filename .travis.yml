language: python
python:
    - "3.5"
before_script:
    - pip install coverage && pip install python-coveralls
install: "python setup.py install"
script: coverage run --source mvol_api tests.py && coverage report
# Working on coveralls support for organization repositories
#after_success:
#    - coveralls
